apiVersion: v1
kind: Service
metadata:
  name: zone-spread-exempt-demo
  namespace: kyverno-ha-demo
  labels:
    app: zone-spread-exempt-demo
spec:
  clusterIP: None
  selector:
    app: zone-spread-exempt-demo
    ha-zone-spread: "enabled"
    ha-zone-spread-exempt: "true"
